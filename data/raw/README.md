# Raw dataset files

`Original Dataset of Employee Attrition.xlsx` is the only spreadsheet used by the project pipeline.

The two Word files document the original survey questions and variable encodings. They are retained as data provenance evidence. The repository does not include the provider's ready-made tree-model or non-tree-model files because this project performs its own preprocessing from the original data.
